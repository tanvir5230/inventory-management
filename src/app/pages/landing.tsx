import { Icon } from '@/components/ui/Icon';

const LandingPage = () => {
  return (
    <div>
      This is the landing page
      <Icon name='circle' />
    </div>
  );
};

export default LandingPage;
